schema:
  entities:
    - type: "Kubernetes Cluster"
      id: ["k8s.cluster.id", "k8s.cluster.name"]
      attributes: ["k8s.cluster.port"]
  events:
    entities:
      - type: "Kubernetes Cluster"
        conditions: ["true"]
        context: "log"
        action: "delete"
