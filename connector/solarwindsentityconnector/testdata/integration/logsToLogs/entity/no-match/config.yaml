schema:
  entities:
    - entity: "Snowflake"
      id: ["snowflake.id"]
      attributes: ["attr1"]
  events:
    entities:
      - entity: "Snowflake"
        conditions: []
        context: "log"
        action: "update"
