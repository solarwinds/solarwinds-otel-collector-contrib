schema:
  entities:
    - type: "Snowflake"
      id: ["snowflake.id"]
      attributes: ["attr1"]
  events:
    entities:
      - type: "Snowflake"
        conditions: []
        context: "log"
        action: "update"
