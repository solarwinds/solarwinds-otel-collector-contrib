schema:
  entities:
    - type: "Snowflake"
      ids: ["snowflake.id"]
      attributes: ["attr1"]
  events:
    entities:
      - type: "Snowflake"
        context: "metric"
        action: "update"
