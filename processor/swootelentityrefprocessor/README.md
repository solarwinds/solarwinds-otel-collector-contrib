# SWO OTel EntityRef Processor

<!-- distribution links hack -->
[verified]: https://github.com/solarwinds/solarwinds-otel-collector-releases#verified
[playground]: https://github.com/solarwinds/solarwinds-otel-collector-releases#playground
[k8s]: https://github.com/solarwinds/solarwinds-otel-collector-releases#k8s

<!-- status autogenerated section -->
| Status        |           |
| ------------- |-----------|
| Stability     | [development]: logs, metrics, traces   |
| Distributions | [k8s], [playground], [verified] |
| Issues        | [![Open issues](https://img.shields.io/github/issues-search/solarwinds/solarwinds-otel-collector-contrib?query=is%3Aissue%20is%3Aopen%20label%3Aprocessor%2Fswootelentityref%20&label=open&color=orange&logo=opentelemetry)](https://github.com/solarwinds/solarwinds-otel-collector-contrib/issues?q=is%3Aopen+is%3Aissue+label%3Aprocessor%2Fswootelentityref) [![Closed issues](https://img.shields.io/github/issues-search/solarwinds/solarwinds-otel-collector-contrib?query=is%3Aissue%20is%3Aclosed%20label%3Aprocessor%2Fswootelentityref%20&label=closed&color=blue&logo=opentelemetry)](https://github.com/solarwinds/solarwinds-otel-collector-contrib/issues?q=is%3Aclosed+is%3Aissue+label%3Aprocessor%2Fswootelentityref) |

[development]: https://github.com/open-telemetry/opentelemetry-collector/blob/main/docs/component-stability.md#development
[k8s]: https://github.com/open-telemetry/opentelemetry-collector-releases/tree/main/distributions/otelcol-k8s
[playground]: 
[verified]: 
<!-- end autogenerated section -->

## Overview

The `swootelentityref` processor inserts or removes [OpenTelemetry Entity References](https://github.com/open-telemetry/opentelemetry-specification/blob/main/specification/entities/README.md) on the Resource of each signal it processes.

**Supported signals**: logs, metrics, traces.

### How it works

When `action: insert` is configured, the processor evaluates each `entity_refs` entry against the Resource attributes of every incoming signal (log record, metric resource, or trace resource). An EntityRef is appended to the Resource only when **all** attribute keys listed in `id_keys` are present. This means a single processor instance can hold a full static mapping (e.g. all Kubernetes entity types) and will only populate the entries that are actually supported by the attributes available on each resource.

When `action: remove` is configured, all EntityRefs are cleared from the Resource of each signal.

## Configuration

| Field | Type | Required | Description |
| --------------------- | -------------- | ------------------ | ----------- |
| `action` | string | ✅ | Operation to perform: `insert` or `remove` |
| `entity_refs` | list | required when `action: insert` | List of EntityRef entries to evaluate. Must be empty when `action: remove`. Duplicate types (case-insensitive) are rejected at startup. |
| `entity_refs[*].type` | string | ✅ | Entity type name (e.g. `KubernetesContainer`) |
| `entity_refs[*].id_keys` | list of string | ✅ | Resource attribute keys that identify the entity; all must be present for the EntityRef to be inserted |

## Examples

### Kubernetes container logs — insert all applicable EntityRefs

The following configuration covers the Kubernetes entity types monitored by the SWO K8s Collector. The processor inserts only the EntityRefs whose `id_keys` are all present in the signal's Resource attributes:

```yaml
processors:
  swootelentityref/container_logs:
    action: insert
    entity_refs:
      - type: KubernetesContainer
        id_keys: [sw.k8s.cluster.uid, k8s.namespace.name, k8s.pod.name, k8s.container.name]
      - type: KubernetesPod
        id_keys: [sw.k8s.cluster.uid, k8s.namespace.name, k8s.pod.name]
      - type: KubernetesNode
        id_keys: [sw.k8s.cluster.uid, k8s.node.name]
      - type: KubernetesDeployment
        id_keys: [sw.k8s.cluster.uid, k8s.namespace.name, k8s.deployment.name]
      - type: KubernetesStatefulSet
        id_keys: [sw.k8s.cluster.uid, k8s.namespace.name, k8s.statefulset.name]
      - type: KubernetesDaemonSet
        id_keys: [sw.k8s.cluster.uid, k8s.namespace.name, k8s.daemonset.name]
      - type: KubernetesJob
        id_keys: [sw.k8s.cluster.uid, k8s.namespace.name, k8s.job.name]
      - type: KubernetesCronJob
        id_keys: [sw.k8s.cluster.uid, k8s.namespace.name, k8s.cronjob.name]

service:
  pipelines:
    logs/container:
      receivers: [filelog]
      processors: [k8sattributes, swootelentityref/container_logs]
      exporters: [otlp]
```

### Remove all EntityRefs

```yaml
processors:
  swootelentityref/strip:
    action: remove

service:
  pipelines:
    logs:
      receivers: [otlp]
      processors: [swootelentityref/strip]
      exporters: [debug]
```

## Pipeline placement

This processor declares `MutatesData: true` because it appends to or clears the Resource's EntityRef slice in place. The Collector's fanout connector marks a batch **read-only** before dispatching it to any pipeline branch that shares the batch with a non-mutating consumer. If `swootelentityrefprocessor` receives a read-only batch, `AppendEmpty` and `RemoveIf` will panic and crash the collector process.

**Safe**: linear pipeline (single consumer per batch).

```text
receiver → swootelentityref → exporter
```

**Unsafe**: fanout where another branch has a non-mutating consumer.

```text
receiver → fanout connector → swootelentityref → exporter  ← panics on read-only batch
                           → debug exporter (MutatesData: false)
```

To use this processor downstream of a fanout connector, ensure every sibling branch also declares `MutatesData: true`, or insert a cloning component (such as a copy-on-write fan-out connector) before this processor's branch.
