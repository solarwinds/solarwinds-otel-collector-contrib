type: swok8sworkloadtype

status:
  class: processor
  stability:
    development: [metrics]
  distributions: [verified, playground, k8s]
  disable_codecov_badge: true
  codeowners:
    active: [orgs/solarwinds/teams/eng-pub-swi-kube]

tests:
  config:
  skip_lifecycle: true
