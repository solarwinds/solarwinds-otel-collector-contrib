type: solarwinds

github_project: solarwinds/solarwinds-otel-collector-contrib

status:
  class: processor
  stability:
    development: [ metrics, logs, traces ]

resource_attributes:
  sw.otelcol.receiver.name:
    enabled: true
    description: "Integration type."
    type: string
  sw.otelcol.integration.id:
    enabled: true
    description: "Integration ID."
    type: string
  sw.otelcol.collector.name:
    enabled: true
    description: "SolarWinds OTEL Collector name."
    type: string
  sw.otelcol.collector.entity_creation:
    enabled: false
    description: "Defines if entity should be created from signal."
    type: string

tests:
  config:
