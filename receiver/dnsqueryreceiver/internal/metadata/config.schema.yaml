# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for dnsquery metrics.
    type: object
    properties:
      dns_query.query_time_ms:
        description: "DNSQueryQueryTimeMsMetricConfig provides config for the dns_query.query_time_ms metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      dns_query.rcode_value:
        description: "DNSQueryRcodeValueMetricConfig provides config for the dns_query.rcode_value metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      dns_query.result_code:
        description: "DNSQueryResultCodeMetricConfig provides config for the dns_query.result_code metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for dnsquery metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
